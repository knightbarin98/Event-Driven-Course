spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://home/mrbarin/Documents/Event-Driven-Course/microservices/config-server-repository
          clone-on-start: true
    fail-fast: true
